<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          package="isomora.com.greendoctor">

    <dist:module dist:instant="true"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name="isomora.com.greendoctor.flash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="isomora.com.greendoctor.scan2"/>
        <activity android:name="isomora.com.greendoctor.MainActivity"/>
        <activity android:name="isomora.com.greendoctor.result"/>
        <activity android:name="isomora.com.greendoctor.mess"/>
        <activity android:name="isomora.com.greendoctor.diseases"/>
    </application>

</manifest>